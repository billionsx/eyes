# ЭФИР НА ДОМЕНЕ · последняя проверка

проверка в сети ещё не выполнялась этим прогоном.
Орган: `bin/livecheck.py` — смотрит `<url из registry/site.json>/data.json`
снаружи и сравнивает отпечаток времени с `dashboard/data.json`.
Вердикт пишется сюда каждым прогоном воркфлоу `eyes` и `eyes-atlas`.
